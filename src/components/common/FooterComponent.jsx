/**
 * @author Alberto González
 *
 */
import { Layout } from "antd";

const { Footer } = Layout;

export const FooterComponent = () => {

    return (
        <Footer>
            <span>"Todos somos muy ignorantes. Lo que ocurre es que no todos ignoramos las mismas cosas." - Albert Einstein </span>
        </Footer>
    );
}